# NEON GRAVITY: ORBITAL 総合技術仕様書

## 1. プロジェクト概要
**NEON GRAVITY: ORBITAL** は、HTML5 CanvasとJavaScript (ES6+) で構築された、見下ろし型・全方位シューティングゲームです。
ネオン管のような発光表現（グローエフェクト）と、慣性を伴う物理挙動、動的に歪む背景グリッドを特徴としています。BaaSとしてFirebase Firestoreを導入し、リアルタイムなワールドランキングシステムを実装しています。

### 1.1 技術スタック
* **言語**: HTML5, CSS3, JavaScript (ES6 Modules)
* **描画**: HTML5 `<canvas>` API (2D Context)
* **音響**: Web Audio API (動的シンセサイザー), HTMLAudioElement (BGM)
* **バックエンド**: Firebase Firestore (v9)

---

## 2. システムアーキテクチャ

### 2.1 状態遷移 (State Machine)
ゲームの進行はグローバル変数 `gameState` によって管理され、メインループ内で分岐処理が行われます。

| ステート名 | 概要 | 遷移条件 |
| :--- | :--- | :--- |
| `TITLE` | タイトル画面、メニュー待機状態 | 初期状態、またはゲームオーバー/ポーズからの「QUIT」 |
| `PLAYING` | メインゲームプレイ中 | TITLE画面から「START GAME」選択 |
| `PAUSED` | 一時停止中（UIとBGMが停止） | PLAYING中にポーズボタン押下 / 画面フォーカスが外れた時 |
| `DYING` | 自機撃破時のスローモーション演出 | PLAYING中に自機のシールド(HP)が0になる |
| `GAMEOVER_UI` | スコア表示・ネーム入力・ランキング送信 | DYING演出終了後、またはENDING演出後 |
| `ENDING` | ラスボス撃破後のクリア演出 | Stage 10クリア時 |
| `OST` | サウンドトラック視聴モード | TITLE画面から「SOUNDTRACK」選択 |

### 2.2 メインループ構造
`requestAnimationFrame` を用いたフレームベースの更新ループ (`loop`関数) で動作します。
1.  **入力の取得**: キーボード、タッチ操作、ゲームパッドの入力を毎フレーム取得・正規化。
2.  **座標更新 (`update`)**: 自機、敵機、弾、パーティクル、グリッドの物理演算と座標更新。
3.  **衝突判定**: 円形交差判定（ピタゴラスの定理 `Math.hypot`）を用いた軽量な当たり判定。
4.  **カメラ補間**: 自機（およびボス）の位置を追従するためのLerp（線形補間）計算。
5.  **描画 (`draw`)**: Canvasへのレンダリング。

---

## 3. プレイヤー・操作仕様

### 3.1 プレイヤーステータス (`player` オブジェクト)
* **移動速度**: 基本速度 `PLAYER_BASE_SPEED` × 慣性計算。
* **シールド(HP)**: 初期値 100。敵弾や敵本体との接触で減少し、0でゲームオーバー。
* **武装レベル (`weaponLevel`)**: Lv.1 〜 Lv.7。レベル上昇で発射弾数（2Way〜8Way）と範囲が拡大。

### 3.2 操作マッピング
クロスプラットフォームに対応するため、複数の入力方式をシームレスにサポートしています。

| アクション | キーボード | ゲームパッド | モバイル (タッチ) |
| :--- | :--- | :--- | :--- |
| **移動** | Arrow キー | 左アナログスティック / D-Pad | 画面左半分のスワイプ (仮想Lスティック) |
| **射撃/エイム** | Space / Z キー (移動方向へ射撃) | 右アナログスティック / Aボタン | 画面右半分のスワイプ (仮想Rスティック) |
| **ボム(特殊)** | X キー | B / X / RT ボタン | 画面右下の BOMB ボタン |
| **ポーズ** | Esc キー | Start / Options ボタン | 画面右下の || ボタン |

### 3.3 特殊アクション (サテライト・ボム)
「クリスタル」アイテムを回収すると、自機の周囲に最大12個の「サテライト（衛星）」が追従します。
ボム入力を行うと、サテライトを全消費して**画面内の敵と弾を消去する巨大な衝撃波**を放ちます。衝撃波の半径は消費したサテライトの数に比例します。

---

## 4. アイテム・パワーアップ仕様
敵を撃破すると、確率（`DROP_RATES`）で以下のアイテムがドロップします。

| アイテム種別 | 表示 | 効果・仕様 |
| :--- | :--- | :--- |
| **Level Up** | 緑の[W] | 武器レベルが1上昇。各ステージ最大2個までの出現制限あり。 |
| **Laser** | シアンの[L] | 一定時間(`LASER_DURATION`)、敵弾をかき消す高威力の貫通レーザーを放つ。 |
| **Invincible** | 黄色の[I] | 一定時間(`INVULN_DURATION`)、ダメージを無効化し、体当たりで敵を破壊可能。 |
| **Shield** | シアンの[S] | シールドを10回復。HPが30未満の時はドロップ率が上昇(20%)する。 |
| **Crystal** | 緑の結晶 | スコア加算 ＆ ボム用のサテライトを1つストックする。 |

---

## 5. 敵機（エネミー）AI・行動仕様
各敵ユニットは `updateEnemies()` 関数内の `switch` 文によって固有のAIが実行されます。

### 5.1 通常エネミー
| 種類 (type) | 特徴とAIロジック |
| :--- | :--- |
| **Triangle** | `V`, `W`, `H`のいずれかのフォーメーションを組んで出現。リーダー機が自機を追従し、随伴機はリーダーの相対座標を維持する。リーダー撃破で個別に追尾を開始。 |
| **Tadpole** | 極めて高い加速力と極端に低い摩擦（滑りやすい）を持つ。自機めがけて突進し、勢い余って通り過ぎる一撃離脱戦法をとる。 |
| **Cube** | 弾は撃たず、ゆっくりと漂うアイテムキャリア。撃破すると必ずパワーアップアイテムをドロップする。 |
| **Hunter** | 4つのアームを持つ機体。①高速接近 → ②自機をエイムして3連射 → ③後退して距離を取る、という3フェーズのステートマシンで動作。 |
| **Asteroid** | 岩石。自機をゆっくり追尾する。破壊すると1段階小さいサイズに2分裂する（最大3段階）。敵弾を吸収する盾にもなる。 |
| **Bubble** | 泡状の敵。Asteroidと似ているが、弾力係数が高く、他の敵と衝突すると跳ね返る。破壊時の分裂演出が異なる。 |
| **Jellyfish** | クラゲ。サイン波によるパルス（脈動）で、加速と減速を繰り返しながら泳ぐように近づく。 |
| **Spark Jelly**| Jellyfishの亜種（帯電状態）。収縮時にエネルギーをチャージし、ピークに達すると自機へ向かう「衝撃波（円形弾）」を放つ。 |
| **Phantom** | ステルス機。遠距離では透明化(`alpha`低下)して接近。一定距離に入ると実体化し、予告線（レーザーサイト）を出した直後に超高速弾を放ち、再び離脱・透明化する。 |
| **Eclipse** | 重力兵器。自機をゆっくり引き寄せる。時間経過で「全方位ばらまき弾」「高速レーザー」「Fighterビット召喚」をローテーションで行う。 |
| **Dragon** | 多関節の大型機体。頭部の軌跡を後続のセグメントが追従するIK（インバース・キネマティクス）風の動き。各節から自機狙いの弾を放つ。 |

### 5.2 ボスエネミー
ステージの規定撃破数に達すると、ワームホールから出現します。

#### 通常ボス (`boss`)
* **構造**: Stage 1〜8で多角形の角数（3角形〜10角形）が増加するモジュール構造。
* **行動**: 
    1.  **フェーズ1**: 連続回転しながら全方位にレーザー弾をばらまく。
    2.  **フェーズ2**: 回転を止め、自機の未来位置を予測（リード射撃）した警告付き狙撃弾を3連射。
    3.  **フェーズ3**: 高い誘導性能を持つホーミングミサイルを発射。

#### 最終ボス (`battleship` / GENESIS-ARK)
* **出現**: Stage 10の開始時に固定出現。
* **行動**:
    * 基本は超重厚な移動。定期的に「ワームホールからのPhantom召喚」や「Asteroid召喚」「Fighter展開」を行う。
    * **発狂モード**: HPが50%を切ると赤く発光し、移動をほぼ停止。代わりに1秒間隔でランダムな雑魚敵を高速で召喚し続ける弾幕・物量戦へ移行する。

---

## 6. ステージ構成・進行仕様
* **Stage 1 〜 8**: 規定数（`STAGE_ENEMY_COUNTS`）の敵を倒すとボスが出現。
* **Stage 9 (BOSS RUSH)**: 過去のボス8体が約3秒間隔で次々と出現。同時に雑魚敵（ワームホール）も絶え間なく出現する耐久ステージ。
* **Stage 10 (FINAL MISSION)**: 巨大戦艦のみが登場。雑魚敵は戦艦のAIによって無限に召喚される。ボス撃破でゲームクリア(`isStageClear = true`)。

---

## 7. 描画・エフェクトシステム (Rendering)

### 7.1 ネオン・レンダリング (Neon Glow)
高負荷な `shadowBlur` の使用を必要最小限に抑え、Canvasの `globalCompositeOperation = 'lighter'` を用いた加算合成と、線の太さ・透明度を変えた複数回の `stroke()` の重ね描画により、軽量かつ美しいネオン発光を実現しています。

### 7.2 グリッド・ディストーション (空間の歪み)
`initGrid()` で画面全体に等間隔の点(`gridPoints`)を生成。
爆発や被弾時、`distortGrid(x, y, force, radius)` 関数を呼び出すことで、指定座標の周囲のグリッド点に速度(`vx, vy`)を与えます。
各点は毎フレーム、元の座標(`ox, oy`)に向かってバネの引力（フックの法則）で戻る演算がされ、空間が波打つような視覚効果を生み出します。

### 7.3 動的カメラシステム
`updateCamera()` により、自機が常に画面の中心〜やや下(`CAMERA_Y_OFFSET`)になるよう追従します。
ボス出現時は、自機とボスの中間地点を計算し、ボスが画面内に収まるよう自動でズームアウト(`cameraScale` の縮小)が行われます。

---

## 8. オーディオシステム (AudioSys)
* **BGM**: HTML5 `<audio>` タグによる再生。`OST` モードでのシークバー操作、曲のクロスフェードアウト処理を実装。
* **SE (効果音)**: `AudioContext` を用いた**プロシージャル・シンセサイザー**。
    * 音声ファイルを読み込まず、JavaScriptのオシレーター（正弦波、ノコギリ波など）とフィルター、エンベロープ（音量減衰）を組み合わせてリアルタイムに波形を生成。
    * ホワイトノイズのバッファも自己生成し、爆発音（低周波パスフィルタ）等に使用。

---

## 9. ランキング・Firebase連携
* **Firebase構成**: Firebase v9 SDK (Modular型) の Firestore を使用。
* **コレクション**: `neon_gravity_scores`
* **データ構造**:
    ```json
    {
      "name": "string (最大10文字)",
      "score": "number",
      "stage": "number または string ('CLEAR')",
      "timestamp": "serverTimestamp"
    }
    ```
* **送信ロジック**: 
    1.  ゲーム終了時、スコアがトップ10にランクインしているか確認。
    2.  ランクインしていれば名前入力UIを表示。Stage 10クリア時は `stage` フィールドに `"CLEAR"` 文字列を格納して送信。
    3.  送信成功後、11位以下の古いデータを自動削除するクリーンアップ処理を実行（DB容量の肥大化防止）。

---

## 主要パラメータ一覧（バランス調整用）

ゲームの難易度やテンポを調整する際は、ソースコード上部にある以下の定数（const）を変更します。

### 1. 基本システム・プレイヤー関連
| 変数名 | 初期値 | 役割 |
| :--- | :--- | :--- |
| `G_SCALE` | 0.7 | 描画される全てのオブジェクトの基本倍率（当たり判定にも比例して影響します）。 |
| `SPEED_SCALE` | 0.25 | 自機・敵機・弾丸など、ゲーム全体の基本移動速度の倍率。 |
| `PLAYER_BASE_SPEED` | 12 | 自機の基本移動速度。 |
| `PLAYER_BASE_SHIELD` | 100 | 自機の最大HP（シールド量）。 |
| `MAX_WEAPON_LEVEL` | 7 | 通常ショットの最大強化レベル。 |
| `INVULN_DURATION` | 400 | 無敵アイテム取得時の効果時間（フレーム数。400 = 約6.6秒）。 |
| `LASER_DURATION` | 400 | レーザーアイテム取得時の効果時間（フレーム数。400 = 約6.6秒）。 |

### 2. ステージ・難易度スケーリング
ステージが進むごとに敵が強くなる割合を設定します。
| 変数名 | 初期値 | 役割 |
| :--- | :--- | :--- |
| `DIFFICULTY_CONFIG.SPEED_INC` | 0.08 | 1ステージごとの敵速度上昇率 (+8%)。 |
| `DIFFICULTY_CONFIG.HP_INC` | 0.5 | 1ステージごとの敵HP上昇補正。 |
| `DIFFICULTY_CONFIG.BULLET_SPEED_INC` | 0.06 | 1ステージごとの敵弾速上昇率 (+6%)。 |
| `STAGE_ENEMY_COUNTS` | `[60, 80...360]` | 各ステージでボスが出現するまでに倒す必要のある敵の総数。 |
| `STAGE_MAX_ON_SCREEN` | `[15, 20...60]` | 各ステージで画面内に同時に存在できる敵の最大数。 |

### 3. アイテムドロップ率 (`DROP_RATES`)
敵を倒した際に、各種アイテムが出現する確率です。これに外れた場合は「クリスタル」が出現します。
| 変数名 | 初期値 | 役割 |
| :--- | :--- | :--- |
| `LEVEL` | 0.30 | 武装強化アイテム（W）のドロップ率 (30%)。※各ステージ最大2個までの制限あり |
| `LASER` | 0.10 | レーザーアイテム（L）のドロップ率 (10%)。 |
| `INVINCIBLE` | 0.05 | 無敵アイテム（I）のドロップ率 (5%)。 |
| `SHIELD_LOW` | 0.20 | 自機のHPが30未満の時のシールド回復（S）ドロップ率 (20%)。 |
| `SHIELD_NORM` | 0.05 | 通常時のシールド回復（S）ドロップ率 (5%)。 |

### 4. 弾丸の性能 (`BULLET_CONFIG`)
自機および敵機が発射する弾の速度と寿命（射程）です。
| 変数名 | 初期値 | 役割 |
| :--- | :--- | :--- |
| `PLAYER.SPEED` | 32.0 | 自機の通常ショットの速度。 |
| `PLAYER.LIFE` | 120 | 自機の通常ショットの寿命（射程距離）。 |
| `ENEMY_NORMAL.SPEED` | 10.0 | 雑魚敵の弾の基本速度。 |
| `BOSS_LASER.SPEED` | 9.0 | ボスのばら撒きレーザーの基本速度。 |
| `BOSS_HOMING.SPEED` | 10.0 | ボスの誘導ミサイルの基本速度。 |

### 5. 敵の基本移動速度 (`ENEMY_SPEEDS`)
※ここに設定された値に `SPEED_SCALE` と `ステージ補正` が掛け合わされて最終的な速度になります。
| 変数名 | 初期値 | 役割 |
| :--- | :--- | :--- |
| `TADPOLE` | 14 | タッドポール（白）。全敵中最速。 |
| `TRIANGLE` | 8 | トライアングル（紫）。標準的な速度。 |
| `HUNTER` | 7 | ハンター（橙）。接近時の速度。 |
| `DRAGON` | 5 | ドラゴン（赤）。中ボス。 |
| `BUBBLE` | 3.5 | バブル（水色）。フワフワ漂う。 |
| `CUBE` | 2.5 | キューブ（緑）。アイテムキャリア。遅い。 |
| `JELLYFISH` | 2.5 | クラゲ。基本は遅いが脈動で加速。 |
| `PHANTOM` | 2.0 | ファントム。ステルス時の基本速度。 |
| `ECLIPSE` | 1.5 | エクリプス。重力源。非常に遅い。 |
